# Basic API created in ZRP code challenge to return Pokemón abilities

## Setup

Install dependencies with:

`npm install`

## Running

Run the following command to make the app listen on port 3000

`node index.js`
